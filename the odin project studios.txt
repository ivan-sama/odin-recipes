intalar git.

Step 2.2: Setup Git
For Git to work properly, we need to let it know who we are so that it can link a local Git user (you) to GitHub. When working on a team, this allows people to see what you have committed and who committed each line of code.

crear key hhs (25%)

crear repositorio (27%)https://www.theodinproject.com/lessons/foundations-git-basics

justify-content: center;
flex-start
flex-end
space-around
space-between


align-items: center;
flex-start   (top)
flex-end      (bottom)
stretch
baseline


gh auth login - para iniciar sesion con github y poder clonar y hacer push a repositorios de github
Solo se ocupa hacer una sola vez aunque uses mas repositorios
git clone URL - clona el repositorio
git add . - Añade todos los archivos cambiados a el commit
git commit -m "mensaje" - aplica los cambios a un nuevo commit con el mensaje "mensaje"
git push origin main - sube los cambios al servidor remoto origin en la rama main